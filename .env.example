# Copy to .env.local for local dev, and set the same two in Vercel → Project → Settings → Environment Variables
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY

# Optional — only needed for the "Draft with AI" button (serverless function). Set in Vercel, not committed.
# ANTHROPIC_API_KEY=sk-ant-...
